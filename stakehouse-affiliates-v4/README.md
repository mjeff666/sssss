# StakeHouse Affiliates (Next.js + Tailwind)

Vercel-ready landing (App Router).

## Local
```bash
npm i
npm run dev
```

## Deploy (Vercel)
- Import repo to Vercel
- Root Directory: repository root (where `package.json` is)
- Build: `next build`, Output: `.next`
